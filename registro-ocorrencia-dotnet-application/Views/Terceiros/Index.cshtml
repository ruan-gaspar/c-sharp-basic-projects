@model IEnumerable<RegistroOcorrencias.Models.Terceiros>
@{
    ViewData["Title"] = "Terceiros";
}

<div class="container">
    <h2 class="text-white mb-4">Terceiros</h2>

    <a asp-action="Create" class="btn btn-success mb-3">Novo Terceiro</a>

    <table class="table table-dark table-striped">
        <thead>
        <tr>
            <th>Nome</th>
            <th>CPF</th>
            <th>Telefone</th>
            <th>Ações</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var terceiro in Model)
        {
            <tr>
                <td>@terceiro.NomeCompleto</td>
                <td>@terceiro.Cpf</td>
                <td>@terceiro.Telefone</td>
                <td>
                    <a asp-action="Details" asp-route-id="@terceiro.Id" class="btn btn-info btn-sm">Detalhes</a>
                    <a asp-action="Edit" asp-route-id="@terceiro.Id" class="btn btn-warning btn-sm">Editar</a>
                    <a asp-action="Delete" asp-route-id="@terceiro.Id" class="btn btn-danger btn-sm">Excluir</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
</div>